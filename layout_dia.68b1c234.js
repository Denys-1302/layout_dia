"use strict";let slides=document.querySelectorAll(".slide"),current=0;document.querySelector(".next").addEventListener("click",()=>{slides[current].classList.remove("active"),current=(current+1)%slides.length,slides[current].classList.add("active")}),document.querySelector(".prev").addEventListener("click",()=>{slides[current].classList.remove("active"),current=(current-1+slides.length)%slides.length,slides[current].classList.add("active")});let menuToggle=document.querySelector(".icon--menu"),menu=document.querySelector(".menu__nav"),menuLinks=document.querySelectorAll(".menu__nav .nav__link");menuToggle.addEventListener("click",()=>{menu.classList.toggle("active")}),menuLinks.forEach(e=>{e.addEventListener("click",()=>{menu.classList.remove("active")})});let form=document.getElementById("contact-form");form.addEventListener("submit",e=>{e.preventDefault(),form.reset()});
//# sourceMappingURL=layout_dia.68b1c234.js.map
